DB_HOST=db
DB_PORT=5432
DB_NAME=roomie_kz
DB_USER=roomie_user
DB_PASSWORD=roomie_pass

JWT_SECRET=change_me_in_production
PORT=5000
CLIENT_URL=http://localhost:5173

# ---------- Email verification (SMTP) ----------
# Leave blank to run the mailer in MOCK mode (codes printed to logs + returned as devCode).
# Gmail: enable 2-Step Verification, create an App Password, use it as SMTP_PASS.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=
